# User Dashboard Environment Variables
# Copy this file to .env and customize as needed

# Node Environment (production/development)
NODE_ENV=production

# User and Group IDs (optional, defaults to 1000)
UID=1000
GID=1000

# Dashboard Port (change if 4000 is already in use)
# Update docker-compose.yml ports section if changed
DASHBOARD_PORT=4000
